# .ebextensions/01_install_deps.config for aws
commands:
  01_clean_pip_cache:
    command: "rm -rf /root/.cache/pip"
    ignoreErrors: true
  02_clean_tmp:
    command: "rm -rf /tmp/*"
    ignoreErrors: true
  # Commands 03_list_staging_dir, 04_copy_reqs, 05_install_deps REMOVED
  # Relying on standard Beanstalk Python platform hook to install requirements.txt